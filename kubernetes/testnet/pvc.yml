apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: casperparser-testnet-pg-volume
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Gi